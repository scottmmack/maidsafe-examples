#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gitbook-pdf/bin/gitbook-pdf.js" "$@"
  ret=$?
else 
  node  "$basedir/../gitbook-pdf/bin/gitbook-pdf.js" "$@"
  ret=$?
fi
exit $ret
